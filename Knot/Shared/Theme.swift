import SwiftUI

/// A small set of visual constants the editor and onboarding share so the
/// app feels consistent across platforms.
enum Theme {
    static let cornerRadius: CGFloat = 12
    static let editorPadding: CGFloat = 16
    static let editorMinHeight: CGFloat = 140
    static let popoverWidth: CGFloat = 380
    static let popoverHeight: CGFloat = 240
}
